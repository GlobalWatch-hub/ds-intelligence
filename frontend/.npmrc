# Route the @globalwatch-hub scope to GitHub Packages. The auth token is NOT
# stored here — it lives in each machine's ~/.npmrc (local dev + the server),
# set with:  npm config set //npm.pkg.github.com/:_authToken <read:packages token>
@globalwatch-hub:registry=https://npm.pkg.github.com
